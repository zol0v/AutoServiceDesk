import { Card, Typography } from 'antd';

export default function NewTicketPage() {
  return (
    <Card>
      <Typography.Title level={3}>New Ticket</Typography.Title>
      <Typography.Paragraph>
        Форма создания новой заявки будет добавлена позже.
      </Typography.Paragraph>
    </Card>
  );
}