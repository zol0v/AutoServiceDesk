import { Card, Typography } from 'antd';

export default function CategoriesPage() {
  return (
    <Card>
      <Typography.Title level={3}>Categories</Typography.Title>
      <Typography.Paragraph>
        Управление категориями будет добавлено позже.
      </Typography.Paragraph>
    </Card>
  );
}