# Backend
# Connection string to PostgreSQL
POSTGRES_CONNECTION=Host=localhost;Port=5432;Database=servicedesk;Username=postgres;Password=postgres

# JWT
JWT_ISSUER=ServiceDesk
JWT_AUDIENCE=ServiceDeskClient
JWT_SIGNING_KEY=CHANGE_ME_TO_A_LONG_RANDOM_SECRET

# Frontend (Vite)
VITE_API_BASE_URL=http://localhost:5000
